https://github.com/TNOTawa/JinrikiHelper 我是这个项目的管理者，这个项目也是我的Vibe Coding练手项目，其已经因为难以维护而归档。后续项目为同github账号的 utau-jrkhh ，我的主要想法有这些： --- 1、默认仅音源原音设定以及拼字相关功能，自动标注的模型作为可拓展包 2、jrh格式的OpenUTAU原音标注定制音素器音源。每个字为如下标记格式‘1-2-ni-hao-a’、'1-2-ko-n-ba'。每个项代表的内容为'第x句-句中第几个音素-前一个音素-该音素-后一个音素'。对于句中的首字和末字的情况处理示例：'1-1-R-ni-hao'、'1-3-hao-a-R'（用R作休止）。这样子可以不再依赖于 UTAU 原版音源格式 oto.ini 的局限。 3、jrh格式音源额外记录与原素材本体的时间轴映射，便于后期将素材音视频对轨 4、jrh格式音源的标注方式应可无缝转换为各软件适配格式，如传统 UTAU 的 CV、CVVC、VCV、适用于喜欢 DAW 拼字的整句便捷搜索 5、新增呼吸音的标注 6、ASR模型允许替换为阿里系模型的本地或在线API、同时兼容本地或API的Whisper；MFA 由于过于笨重我决定不再兼容，而是使用 SOFA，即使针对歌唱语音而不是说话，但我也觉得比 MFA 方便些。 7、支持英语的标注，参考UTAU英语音源使用的ARPA或Xsampa表的音素。 8、第一兼容的自定义音素器，应当会基于音素的连续性进行选取拼接（如，优先连续音素，然后同辅音/元音音素，流畅度依次往下，越往下选取的顺序越靠后） --- 你对此怎么想？和我进行多轮讨论。记得先将这两个项目都看个遍

展开收起

  

#### ChatGPT 说：

思考了 4m 22s

我把两个仓库当前默认分支的目录、README/AGENTS、核心源码和格式文档都过了一遍；没有逐条审计历史提交。

## 总判断

你的方向是对的，而且已经不是“重写 JinrikiHelper”，而是在做一个更合理的新产品：

> **以 JRH 为母版数据格式的音源素材管理、标注、检索与编译工具。**

旧项目的问题并不只是 MFA 太重，而是把 VAD、Whisper、文本处理、MFA 环境管理、音频切片、格式转换和 GUI 串成了一条固定流水线。`pipeline.py` 自己负责模型加载和工作流，另有功能重叠的 `audio_processor.py`；仅 MFA 调用模块就需要处理 Windows 外挂环境、Conda/Mamba 探测、依赖安装、进程取消和临时目录等大量基础设施问题。旧格式中的 `meta.json` 也主要记录 Whisper/MFA 配置，而不是稳定的音源语义。[GitHub+3GitHub+3GitHub+3](https://github.com/TNOTawa/JinrikiHelper/raw/refs/heads/main/src/pipeline.py)

新项目目前轻了很多，但内部核心仍然是：

```
OtoBank└── OtoEntry    ├── wav_filename    ├── alias    ├── offset    ├── consonant    ├── cutoff    ├── preutterance    └── overlap
```

GUI 直接持有和修改 `OtoBank`，拼字窗口里同时存在 UI、日语罗马字表、辅音降级规则、选样和音频拼接逻辑，批量拼字又直接导入单字拼接窗口的内部函数。继续往这里添加 JRH、英语、ASR 和 SOFA，极有可能重新走回旧项目的维护困境。[GitHub+3GitHub+3GitHub+3](https://github.com/TNOTawa/utau-jrkhh/raw/refs/heads/main/src/oto_parser.py)

## 对你八点想法的具体意见

### 1\. 默认不附带模型

**非常赞成，而且这是新项目最重要的边界。**

基础安装应该离线、无账号、无 GPU 也可以完成：

-   导入素材；
-   人工标注；
-   原音设定；
-   音素检索；
-   拼字；
-   JRH 保存；
-   导出各兼容格式。

ASR 和自动对齐应当是可卸载的 Provider，而不是主程序依赖。最好让模型插件运行在独立进程，通过 JSON-RPC/标准输入输出与主程序通信，避免某个 PyTorch、CUDA 或 Transformers 版本再次拖垮整个应用。

建议至少拆成三个不同接口，而不是统称“自动标注模型”：

```
TranscriberProvider   ASR：音频 → 文本G2PProvider           文本 → 音素序列AlignerProvider       音频 + 音素序列 → 音素边界
```

### 2\. `1-2-ni-hao-a` 的 JRH 标记

**概念正确，但不应把这条字符串本身作为底层数据。**

它可以是显示别名或导出别名，但真正存储应该是：

JSON

```
{  "utterance_id": "utt_01",  "ordinal": 2,  "left_unit": "ni",  "current_unit": "hao",  "right_unit": "a"}
```

原因有三个：

第一，句子中插入或删除一个单位后，`1-2` 会全部变化，所以还要有永不变化的 `segment_id`。

第二，你的例子目前更接近“音节或拼接单位”，而不是真正的音素。比如 `hao` 至少还能分成声母和韵母。建议统一称为 **unit**，并同时记录：

JSON

```
{  "surface": "好",  "unit": "hao",  "phones": ["h", "ao"],  "onset": ["h"],  "nucleus": ["ao"],  "tone": 3}
```

第三，不能用 `R` 同时表示休止和英语音素。在 ARPAbet 中，`R` 本身就是 /ɹ/。[GitHub+1](https://github.com/cmusphinx/cmudict-tools/blob/master/accents/en-GB-x-rp.csv?utm_source=chatgpt.com)

建议底层使用：

JSON

```
"left_unit": null,"boundary": "start"
```

人类可读别名再生成成：

```
1-1-BOS-ni-hao1-3-hao-a-EOS
```

### 3\. 原素材时间轴映射

这是我认为 **JRH 最有价值、也最有差异化的一点**。

但不要只记录：

```
原视频 123.45 秒
```

建议至少记录：

JSON

```
{  "asset_id": "src_01",  "asset_hash": "sha256:...",  "stream_index": 1,  "source_time_base": "1/48000",  "source_start_pts": 5925600,  "source_end_pts": 5932800}
```

如果一个音频是两个片段拼出来的，就记录多个 provenance span，而不是一个起止时间。这样将来才能真正做到：

-   回到原视频定位；
-   音视频重新对轨；
-   修改切分后重新生成；
-   判断素材文件是否被替换；
-   查看一个合成音来自哪些原始位置。

原始素材应保持不可变，裁剪 WAV、降噪结果、拼接结果都属于派生资源。

### 4\. 转换为 CV、CVVC、VCV 和整句搜索

建议把“无缝转换”换成：

> **JRH 是母版，其他格式是经过验证的编译目标。**

因为 CV、CVVC、VCV 的素材覆盖并不等价。缺少某个 VC 过渡时，程序无法通过格式转换凭空创造出来。

因此每次导出都应该产生两个东西：

```
export/oto.iniexport-report.json
```

报告应告诉用户：

-   完整生成多少项；
-   哪些项使用了降级候选；
-   哪些音素缺失；
-   哪些字段在 oto.ini 中无法表达；
-   哪些样本需要真实音频拼接；
-   哪些只是复用原素材的不同时间范围。

一句话概括：

> **JRH 是源代码，oto.ini 是编译产物。**

整句 DAW 搜索也不应作为一种“音源格式”，而应是 JRH 的索引功能：按原文、单字、音节、音素序列、音素 n-gram、来源视频和时间范围搜索。

### 5\. 呼吸音

建议不要把呼吸简单塞进普通音素表，而是设为一种事件：

JSON

```
{  "kind": "breath",  "subtype": "inhale",  "voicing": "unvoiced",  "intensity": 0.62}
```

初期可支持：

```
inhaleexhaleaspirated_releasemouth_noiselaughnonverbal
```

导出到传统音源时，再映射成 `br`、`inh`、`exh` 等别名。SOFA 本身也有静音/非周期声音检测及 AP/SP 一类处理思路，可以作为自动建议，但仍应允许人工修正。[GitHub](https://github.com/qiuqiao/SOFA)

### 6\. 阿里系、Whisper 与 SOFA

这个组合是合理的。

现在 FunASR 已经适合做本地 Provider，并支持本地 OpenAI-compatible transcription 服务；阿里云侧也同时存在 Fun-ASR、Qwen-ASR、Paraformer 等录音文件与实时接口，因此可以统一成 HTTP Provider，而不需要在主程序里针对每种 SDK 写一套业务流程。[阿里云帮助中心+3GitHub+3GitHub+3](https://github.com/modelscope/FunASR)

SOFA 比 MFA 更契合你的素材，但必须注意：

> SOFA 不是“音频直接变标注”。

它仍然需要 WAV、对应转写、G2P 或字典，再生成 TextGrid/HTK 等对齐结果。因此正确流程应该是：

```
素材→ ASR 草稿→ 人工修订文本→ G2P→ SOFA 对齐→ 人工修订边界→ 写入 JRH
```

SOFA 只是其中一个可替换的 `AlignerProvider`，不能再次成为整个项目的骨架。[GitHub](https://github.com/qiuqiao/SOFA)

### 7\. 英语 ARPA 与 X-SAMPA

我建议：

-   **第一阶段以 ARPA 为默认英语表示；**
-   X-SAMPA 做导入、显示和导出适配；
-   底层不要直接绑定其中任何一种字符串。

内部可使用带命名空间的表示：

```
en-US:AAen-US:Rxsampa:Aipa:ɑ
```

并映射到共同的发音特征。

另外，ARPAbet 中的 `AH0/AH1/AH2` 最好拆成：

JSON

```
{  "phone": "AH",  "stress": 1}
```

同样，中文声调也不建议继续像旧格式规范那样直接附着在 IPA 元音字符串后面，而应作为独立特征。这样做跨语言选样和降级时会容易很多。

### 8\. 基于连续性的自定义音素器

你的优先级思路正确，但**不能逐音素贪心选择**。

例如每个位置单独选择“当前最好的样本”，最后可能产生五次跨素材拼接；稍微牺牲一个局部候选，却可能让连续三个单位都来自原句，整体反而自然很多。

建议采用动态规划或 Viterbi：

```
每个目标单位 = 一组候选状态候选自身 = 发音匹配成本相邻候选 = 拼接过渡成本全句寻找总成本最低路径
```

候选层级可以设为：

1.  原素材中完整连续的 `left-current-right`；
2.  连续的 `left-current` 或 `current-right`；
3.  当前单位完全相同；
4.  声母、韵母或音素特征相近；
5.  可替代音素；
6.  最后才使用人工拼接生成项。

同层候选再比较：

-   是否来自同一句且时间连续；
-   音高差；
-   音量差；
-   音色/频谱差；
-   时长拉伸比例；
-   边界置信度；
-   ASR/对齐置信度；
-   用户指定优先级。

而且必须保留解释记录，例如：

```
选择 seg_104：完整匹配 hao-a；与上一段来自同一原句；时间间隔 18 ms；比候选 seg_221 少一次跨素材拼接。
```

这会让音素器真正可调试，而不是一个用户无法理解的黑箱。

## 一个必须提前面对的 OpenUtau 限制

按当前 OpenUtau API，我的判断是：

> **仅开发一个自定义 Phonemizer，并不能让 OpenUtau 原生加载纯 JRH 音源。**

Phonemizer 可以读取歌手目录里的自定义资源，但它最终主要返回音素别名；现有实现仍通过 `USinger.TryGetMappedOto()` 找到实际原音和 OTO 参数。API 文档还明确要求 Phonemizer 不要修改 singer。[GitHub+2GitHub+2](https://github.com/stakira/OpenUtau/blob/master/OpenUtau.Core/Api/Phonemizer.cs)

因此现实可行的第一版是：

```
JRH 母版   ↓ 编译隐藏或自动生成的 oto.ini + 必要的派生 WAV   ↓OpenUtau JRH Phonemizer 负责智能选取这些别名
```

也就是说，**不再依赖 oto.ini 作为编辑和存储模型，但暂时仍把它作为 OpenUtau 的运行时兼容层。**

要让 `.jrh` 完全脱离 `oto.ini` 直接被 OpenUtau 加载，就需要改 OpenUtau 的歌手加载器，可能涉及上游 PR 或维护一个分支，不只是写 Phonemizer。

## 我建议的项目结构

```
src/├── core/│   ├── project.py│   ├── asset.py│   ├── utterance.py│   ├── segment.py│   ├── phoneset.py│   └── selection.py├── formats/│   ├── jrh/│   ├── utau_oto/│   ├── textgrid/│   └── daw/├── languages/│   ├── mandarin/│   ├── japanese/│   └── english/├── providers/│   ├── transcription/│   ├── g2p/│   └── alignment/├── audio/└── ui/openutau-jrh-phonemizer/
```

开发顺序则建议是：

1.  先建立与 GUI、OTO、模型都无关的 JRH Core；
2.  让现有 `oto.ini` 可以无损导入 JRH；
3.  完成 JRH 保存、来源时间轴和人工标注；
4.  再做 JRH → OTO 编译及损失报告；
5.  实现 OpenUtau 音素器和全句选样；
6.  加入呼吸音与英语；
7.  最后再接 ASR/SOFA 扩展包。

第一轮我最想和你确认的是：\*\*你希望 JRH 定位为“制作工程与母版格式”，还是希望它本身就是可以直接丢进 OpenUtau 的最终分发格式？\*\*我更推荐前者，这个选择会决定后面大部分架构。

来源

  

#### 你说：

jrh格式可以像你说的那个JSON格式，如果用自定义音素器，就将其编译为我所定义的那个五段式'0-0-x-x-x'格式，同时保留原始JSON用于计算，并可以同时与用户可选的CVVC、VCV、CV版本共存（这些同时用大部分时候是不会出现兼容性问题的） JRH可以定义为母版格式，就像你说的原代码这个比方。不过也可以让后人其他软件主动兼容我们的格式。

  

#### ChatGPT 说：

思考了 49s

这样定位就很清晰了，而且技术上是成立的：

> **JRH JSON 是权威母版；五段式别名、CV、CVVC、VCV 等都是从母版编译出的不同视图。**

自定义音素器读取 JSON 做候选搜索和评分，最后只向 OpenUtau 返回类似：

```
1-2-ni-hao-a
```

OpenUtau 再通过生成的 `oto.ini` 找到实际音频。这里的五段式别名本质上是一个**运行时句柄**，不负责保存全部语义。

## 一、推荐的数据流

```
歌词/音符   ↓JRH 自定义音素器   ↓读取 JRH JSON 中的音素、上下文、时间轴和候选信息   ↓计算最佳素材   ↓返回五段式别名：1-2-ni-hao-a   ↓OpenUtau 通过编译生成的 oto.ini 读取音频
```

同一个 JRH 音源可以同时生成：

```
1-2-ni-hao-a        JRH 五段式hao                 CV- hao               VCVhao a               CVVCao h                VC
```

它们可以共同出现在一个 `oto.ini` 中，指向同一个 WAV 和同一段时间，也可以分别生成后再合并。

你的判断没错：这些别名的形态通常差异很大，大多数时候不会冲突。但编译器仍然应该做到：

-   不允许同名别名被静默覆盖；
-   检测重复别名；
-   显示冲突来自哪个编译目标；
-   允许用户设置优先级或手动改名；
-   生成冲突报告。

## 二、五段式别名不要承担稳定存储职责

JSON 中的每个标注必须有稳定 ID：

JSON

```
{  "id": "unit_7f8c4a12",  "utterance_no": 1,  "unit_no": 2,  "context": {    "left": "ni",    "current": "hao",    "right": "a"  }}
```

编译时生成：

```
1-2-ni-hao-a
```

这里要特别注意一个问题：用户在第一句话中间插入一个字后，后面所有 `unit_no` 都会改变。

因此建议区分两类编号：

JSON

```
{  "id": "unit_7f8c4a12",  "utterance_no": 1,  "unit_no": 2,  "published_no": 2}
```

-   `id`：永远不变，软件内部使用；
-   `utterance_no`、`unit_no`：当前编辑顺序；
-   `published_no`：音源正式发布后冻结，用于生成稳定别名。

音源发布后，删除的编号最好保留空位，不要自动重新排列。例如删除 `1-2` 后仍然保留：

```
1-1-...1-3-...
```

否则旧工程中引用的别名可能全部失效。

当然，自定义音素器通常会重新计算并返回新别名，但用户也可能在 OpenUtau 中手动锁定音素，稳定编号仍然有价值。

## 三、JSON 应保存“声学事实”，而不只是 oto 参数

JRH 既然是母版格式，就不应该只保存：

```
offsetconsonantcutoffpreutteranceoverlap
```

这些是 UTAU 的表达方式，并不是真正通用的声学标注。

更适合保存的是：

JSON

```
{  "source_range": {    "start_sample": 125400,    "end_sample": 153200  },  "landmarks": {    "consonant_start": 125400,    "voicing_start": 130120,    "steady_start": 133500,    "steady_end": 148600,    "release_start": 150200  }}
```

然后不同编译器自行转换：

```
JRH 声学标记├── UTAU oto 参数├── OpenUtau 五段式 oto├── CVVC oto├── VCV oto├── DAW 切片点└── 其他软件格式
```

这样其他软件兼容 JRH 时，不需要先理解 UTAU 的五个参数。

当然，用户手工调整过的目标格式参数也可以保留，但应作为覆盖项：

JSON

```
{  "target_overrides": {    "utau_cvvc": {      "preutterance": 92,      "overlap": 28    }  }}
```

即：

> 通用声学标记负责默认转换，目标格式覆盖负责精细调校。

## 四、建议把“音素”改成更宽泛的 Unit

你给出的：

```
nihaoa
```

严格来说主要是音节或发音单元，不是单音素。例如 `hao` 可以进一步拆成声母和韵母，英语则可能拆成多个 ARPA 音素。

因此 JSON 中建议使用 `unit`：

JSON

```
{  "unit": "hao",  "unit_kind": "syllable",  "phones": ["h", "ao"],  "features": {    "initial": "h",    "final": "ao",    "tone": 3  }}
```

英语可以是：

JSON

```
{  "unit": "HH",  "unit_kind": "phone",  "phones": ["HH"]}
```

或者以音节作为选择单元：

JSON

```
{  "unit": "HH-AH0-L-OW1",  "unit_kind": "syllable",  "phones": ["HH", "AH", "L", "OW"],  "stress": [null, 0, null, 1]}
```

这样五段式的含义可以正式定义为：

```
句编号-句内单元编号-前一选择单元-当前选择单元-后一选择单元
```

而不是强行规定每一项必须是语言学上的单音素。

不同语言配置可以自行决定选择单元：

| 语言  | 默认选择单元 |
| --- | --- |
| 普通话 | 带声调或不带声调的音节 |
| 日语  | 假名、音拍或音素 |
| 英语  | ARPA/X-SAMPA 音素或音节 |
| 自定义语言 | 用户定义 Unit |

## 五、`R` 最好不要作为通用休止标记

在普通话模式中，`R` 基本没有问题。但英语 ARPAbet 中，`R` 本身就是一个有效辅音，因此：

```
R-AH-N
```

无法判断其中的 `R` 是英语 /r/，还是休止。

建议 JSON 内部使用：

JSON

```
{  "left": null,  "boundary": "start"}
```

编译别名时使用不会和正常音素冲突的保留值，例如：

```
1-1-BOS-ni-hao1-3-hao-a-EOS
```

或者：

```
1-1-_-ni-hao1-3-hao-a-_
```

其中 `BOS/EOS` 比单独的 `R` 更明确：

-   `BOS`：句首；
-   `EOS`：句尾；
-   `SIL`：真实静音；
-   `PAU`：句中停顿；
-   `BR`：呼吸音。

这几种状态实际上不应全部混为“休止”。

## 六、推荐的 JRH 包结构

可以让 `.jrh` 同时支持目录形式和压缩包形式。

开发和版本管理时：

```
my_voicebank.jrh/├── manifest.json├── data/│   ├── assets.json│   ├── utterances.jsonl│   ├── units.jsonl│   └── events.jsonl├── phonesets/│   ├── zh-pinyin.json│   └── en-arpabet.json├── workspace/│   └── editor-state.json└── builds/    ├── openutau-jrh/    │   ├── oto.ini    │   ├── alias-map.json    │   └── build-report.json    ├── utau-cv/    ├── utau-cvvc/    └── utau-vcv/
```

发布时可以压缩成单个：

```
my_voicebank.jrh
```

软件可以直接将其当 ZIP 容器读取。

其中：

-   `data/` 是权威数据；
-   `workspace/` 是 JRKHH 编辑器自己的界面信息，不属于公共格式核心；
-   `builds/` 全部可以删除并重新生成；
-   `alias-map.json` 用于把编译别名反查到稳定 ID。

例如：

JSON

```
{  "1-2-ni-hao-a": {    "unit_id": "unit_7f8c4a12",    "target": "openutau-jrh",    "wav": "source_01.wav",    "source_start_sample": 125400,    "source_end_sample": 153200  }}
```

## 七、其他软件兼容 JRH 的前提

你希望未来其他软件主动兼容，这意味着 JRH 不能只是“utau-jrkhh 软件保存出来的 JSON”，而要成为一份独立规范。

至少应当公开：

```
JRH SpecificationJRH JSON SchemaJRH Reference VoicebankJRH Validation ToolJRH Version Migration Guide
```

`manifest.json` 中必须明确：

JSON

```
{  "format": "JRH",  "schema_version": "0.1.0",  "generator": {    "name": "utau-jrkhh",    "version": "0.2.0"  },  "capabilities": [    "source-timeline",    "context-units",    "breath-events",    "target-overrides"  ]}
```

还需要有扩展命名空间：

JSON

```
{  "extensions": {    "org.example.pitch-analysis": {},    "com.someapp.custom-labels": {}  }}
```

其他软件可以忽略不认识的扩展，但仍然读取 JRH 核心数据。这样才不会因为某个程序加入私有字段，就让整个格式不可交换。

## 八、自定义音素器与多格式共存的最终关系

我认为可以正式确定成：

```
                         ┌─ 五段式 oto.ini                         ├─ CV oto.iniJRH JSON → 编译器系统 ────┼─ CVVC oto.ini                         ├─ VCV oto.ini                         └─ DAW 索引/切片     ↑     │读取候选、上下文和来源     │OpenUtau JRH Phonemizer     ↓返回五段式别名
```

五段式格式是 JRH 自定义音素器的**无损运行目标**；CV/CVVC/VCV 是面向现有生态的**有损或半有损兼容目标**。

这个架构的一个好处是：未来有软件原生兼容 JRH 时，它可以完全跳过 `oto.ini`，直接读取：

-   音频来源；
-   声学边界；
-   上下文；
-   候选关系；
-   呼吸事件；
-   音素特征；
-   时间轴映射。

而 OpenUtau 当前则继续通过编译层使用它。

下一步最值得讨论的是：\*\*五段式中的三个 `x` 到底固定表示音节级 Unit，还是允许不同语言配置决定它是音节、音拍或单音素。\*\*我倾向于后者，但普通话默认采用音节级 Unit。

  

#### 你说：

不同语言自定义，而我们优先选择类似于UTAU录音表类的格式，就像无声调拼音、罗马音这样的。以及，用户处理完素材后，本身第一第二项（句编号和句内单元编号）就是永久固定的不会变动，后面的部分只是便于人类可读罢了，没必要这么严苛得对后三项进行规范，音素器返回时也就盯着唯一组合的句编号与句内编号找了。

  

#### ChatGPT 说：

思考了 31s

对，这样就不必把后三项设计得太“语言学化”。我前面确实把五段式本身看得太重了。

## 修正后的定义

五段式：

```
句编号-句内单元编号-前项-当前项-后项
```

实际上分成两层：

```
1-2          永久、唯一的素材坐标ni-hao-a     面向人类的可读说明
```

所以真正的主键是：

```
(sentence_id, unit_id)
```

例如：

```
1-2-ni-hao-a
```

自定义音素器最终只需要确定：

```
我要使用第 1 句第 2 个单元
```

然后直接从编译映射表里取得完整别名，无须反向解析 `ni-hao-a`。

## JSON 可以这样保存

JSON

```
{  "sentence_id": 1,  "unit_id": 2,  "language": "zh-CN",  "unit_system": "pinyin-toneless",  "display": {    "previous": "ni",    "current": "hao",    "next": "a"  },  "phones": ["h", "ao"],  "source": {    "asset_id": "audio-001",    "start_sample": 125400,    "end_sample": 153200  }}
```

编译结果：

JSON

```
{  "1:2": "1-2-ni-hao-a"}
```

音素器内部只操作：

```
1:2
```

输出给 OpenUtau 时才得到：

```
1-2-ni-hao-a
```

这样即使以后显示部分改成：

```
1-2-ni-hao3-a1-2-n-i_h_ao-a1-2-NI-HAO-A
```

也不会影响素材身份。

## 不同语言由语言包定义

我同意优先使用类似 UTAU 录音表的单位，而不是强行追求语言学上的标准音素。

语言包可以定义：

```
普通话：无声调拼音日语：罗马音或假名录音单位英语：ARPAbet 或 X-SAMPA其他语言：对应音源社区常用录音表符号
```

例如：

JSON

```
{  "language_pack": "jrh.zh-pinyin",  "unit_system": "pinyin-toneless",  "version": "1.0"}
```

语言包主要负责五件事：

1.  歌词转换为录音单位；
2.  判断前、当前、后单位；
3.  定义相似单位和降级顺序；
4.  生成可读的五段式尾部；
5.  转换为 CV、CVVC、VCV 等目标格式。

所以 JRH 核心不需要规定 `hao` 究竟是音节、音拍还是音素，只需要知道它是当前语言包定义的一个 `unit`。

## 后三项只需最低限度的技术约束

虽然不需要严格统一语义，但编译到 `oto.ini` 时仍要避免几个字符问题，例如：

```
,=换行
```

这些字符可能破坏 `oto.ini` 的字段结构。

因此可以规定：

> 后三项的内容由语言包自由决定，但必须经过 alias encoder 转换成安全字符串。

例如内部 X-SAMPA 可以保存原始形式，编译别名时进行转义。这个约束只是为了文件能正常读取，不是要统一各语言的标音体系。

## 固定编号后的处理

既然用户处理完成后，前两项永久不变，那么规则可以很简单：

-   新增单元只能取得新编号；
-   已删除编号不再复用；
-   修改文本和标音不改变编号；
-   后三项可以重新生成；
-   原素材时间位置调整也不改变编号。

例如原来有：

```
3-13-23-3
```

删除 `3-2` 后保留：

```
3-13-3
```

后来新增一个单元，应使用：

```
3-4
```

而不是重新占用 `3-2`。这样旧 OpenUtau 工程中的锁定音素不会意外指向另一段素材。

也可以在 JSON 中留下一个很轻量的删除记录：

JSON

```
{  "sentence_id": 3,  "unit_id": 2,  "deleted": true}
```

不过这主要针对已经公开发布过的音源；尚未发布的制作工程可以允许用户整理编号。

## 自定义音素器的实际工作流程

按照你的定义，音素器应该这样运行：

```
歌词↓语言包转换成录音表单位↓根据前项、当前项、后项搜索 JRH JSON↓按连续性和匹配程度选择最佳素材↓得到唯一坐标，例如 8:14↓查编译映射表↓返回 8-14-ko-n-ba
```

这里用于搜索的是 JSON 中的完整信息，最终输出只依赖永久坐标。

甚至可以让编译别名中的后三项完全不参与程序逻辑：

C#

```
var alias = aliasMap[(sentenceId, unitId)];
```

而不是：

C#

```
ParseAlias("8-14-ko-n-ba");
```

这会可靠很多。

## 与 CV、CVVC、VCV 共存

一个素材单元可以编译出多条映射：

JSON

```
{  "source_unit": "1:2",  "aliases": {    "jrh": "1-2-ni-hao-a",    "cv": ["hao"],    "cvvc": ["hao a", "ao a"],    "vcv": ["i hao"]  }}
```

不过这些兼容别名可能不是一对一关系。例如同一个 JRH 单元可能导出多个 CVVC 项，因此更准确的结构是：

JSON

```
{  "source_unit": "1:2",  "compiled_entries": [    {      "target": "jrh",      "alias": "1-2-ni-hao-a"    },    {      "target": "cv",      "alias": "hao"    },    {      "target": "cvvc",      "alias": "hao a"    },    {      "target": "cvvc",      "alias": "ao a"    }  ]}
```

五段式是单元的唯一原生别名；CV、CVVC、VCV 是从它派生的一个或多个兼容别名。

## 当前架构可以正式归纳为

```
JRH 母版├── 永久身份：句编号 + 句内单元编号├── 语言包单位：拼音、罗马音、ARPA、X-SAMPA……├── 可读上下文：前项 + 当前项 + 后项├── 原素材时间轴├── 声学标记└── 导出覆盖参数编译目标├── JRH 五段式├── CV├── CVVC├── VCV└── 其他软件格式
```

这个设计明显比“后三项也是严格主键的一部分”更符合你的使用场景。

下一轮我认为最关键的是明确\*\*“句”在 JRH 中具体代表什么\*\*：是原素材中的一次连续发言/演唱片段，还是用户人为划分的录音表行。我目前更倾向于把它定义为原素材时间轴上的连续片段，因为这样编号、连续性评分和音视频回溯会自然统一。

  

#### 你说：

原素材时间轴上的连续片段。原版人力V助手就有一个轻量的小模型用于识别禁音段并分句。

  

#### ChatGPT 说：

思考了 14s

对，这样“句”的定义就很扎实了：

> **句 = 原素材时间轴上一段连续的有效发声片段，由静音检测初步切分，用户可人工调整。**

它不是语言学意义上的句子，也不是录音表中的一行。即使一段素材只有“你好世界”的一半，只要前后被静音或人工边界隔开，它也可以是一句。

## 静音分句应当属于基础能力

它和 ASR、音素对齐不同。静音检测只负责回答：

```
哪里有声音？哪里是静音？连续素材应该初步切成几段？
```

不需要知道说了什么，所以我认为它可以保留在默认版本中，不必归入“自动标注模型扩展包”。

但可以设计成两级：

```
默认：能量阈值/频谱特征的无模型静音检测可选：轻量神经网络 VAD
```

如果原版的小模型体积很小、依赖简单、许可合适，也可以随软件提供。但核心接口不能绑定它：

Python

```
class SegmentationProvider:    def detect_segments(self, audio, settings) -> list[DetectedSegment]:        ...
```

未来可以替换为：

-   内置轻量模型；
-   Silero VAD；
-   WebRTC VAD；
-   纯能量阈值；
-   其他用户插件。

## 建议的处理流程

```
导入音频/视频↓静音检测↓自动生成连续发声片段↓合并过短间隔、过滤过短噪声↓用户试听并调整句边界↓确认分句↓为每句分配永久句编号↓ASR、G2P、音素对齐或人工标注↓为句内单元分配永久编号
```

这里最重要的是：**自动检测结果只是草稿。**

在用户确认之前，可以随便重新检测、调整阈值和重新切分；确认以后，句编号固定。

## “句”的 JSON 结构

JSON

```
{  "sentence_id": 12,  "source": {    "asset_id": "video-001",    "start_sample": 582400,    "end_sample": 734200  },  "segmentation": {    "provider": "builtin-vad",    "confidence": 0.94,    "original_start_sample": 584100,    "original_end_sample": 731800,    "manually_adjusted": true  },  "units": [    {      "unit_id": 1,      "display": "ni"    },    {      "unit_id": 2,      "display": "hao"    }  ]}
```

其中：

-   `source.start/end`：用户最终确定的真实边界；
-   `original_start/end`：自动检测最初建议的边界；
-   `manually_adjusted`：便于以后区分机器结果和人工结果；
-   `sentence_id`：确认后永久固定。

## 静音和句间空隙也应保留下来

不要只保存句本身，还应保留相邻句之间的空隙：

```
句 1：1.20–3.84 秒静音：3.84–4.31 秒句 2：4.31–7.02 秒
```

这样后期回到视频时间轴时，不会出现累计误差，也便于判断：

-   两句是否应该合并；
-   中间是不是呼吸；
-   是真正静音还是伴奏间隔；
-   是否需要从句尾提取呼吸音。

建议静音检测输出的不只是发声区间，还包括：

JSON

```
{  "regions": [    {      "kind": "speech",      "start_sample": 10000,      "end_sample": 50000    },    {      "kind": "silence",      "start_sample": 50000,      "end_sample": 62000    }  ]}
```

## 合并规则需要允许用户调整

检测到短静音时，不一定要立即分句。例如一句话中间的换气或停顿：

```
“我今天……去了学校”
```

模型可能识别成两个活动区间，但用户可能希望它仍然是一句。

因此静音分句至少需要这些参数：

```
最短有效发声时长最短分句静音时长短静音合并阈值句首保留长度句尾保留长度最长连续句长度
```

例如：

```
小于 80 ms 的发声：视为噪声小于 250 ms 的静音：自动连接超过 600 ms 的静音：建议分句句首、句尾各保留 100 ms
```

数值可以提供默认值，但不应写死。

## 呼吸音与分句的关系

呼吸经常会被 VAD 当成：

-   无声；
-   噪声；
-   很短的有效发声。

所以静音分句阶段最好先使用比较宽松的检测，避免直接删除低能量区域。可以将不确定区域标成：

```
speechsilencepossible_breathunknown
```

然后呼吸音检测插件或用户再进一步确认。

尤其不能在导入阶段就执行破坏性静音裁剪，否则后面想标注呼吸音时，原始素材已经丢失。分句只记录时间范围，不直接改变源文件。

## 编号生命周期

按你的规则，可以简化为两个状态：

### 草稿阶段

```
临时句 A临时句 B临时句 C
```

用户可以任意分割、合并和移动。

### 确认阶段

```
句 1句 2句 3
```

确认后：

-   调整边界不改变句编号；
-   修改文字不改变句编号；
-   重新对齐不改变句编号；
-   修改后三项不改变句内单元编号。

如果已经确认的句后来需要拆分，比较自然的做法是：

```
原句 12 保留给其中一个主要片段新拆出的片段获得新的句编号 35
```

不重新排列整个音源。

## 这也会直接帮助音素器连续选择

由于“句”本身就是原素材连续片段，自定义音素器判断连续性会非常简单。

例如候选为：

```
12-312-412-5
```

程序可以立即知道它们来自同一原片段，而且句内编号连续。再结合实际时间：

```
12-3 的结束时间 ≈ 12-4 的开始时间12-4 的结束时间 ≈ 12-5 的开始时间
```

就可以给予最高优先级。

所以连续性不能只看编号，还应同时检查时间轴。因为用户可能删除过中间单元，出现：

```
12-3 → 12-5
```

但时间上仍然连续；反过来，编号连续也可能因为人工边界调整而存在较大间隔。

可以将连续性定义为：

```
同一句+ 来源时间顺序正确+ 相邻时间差足够小= 原素材连续候选
```

这样比仅依赖录音表符号更可靠。

目前已经可以形成一个明确的三层结构：

```
素材 Asset└── 连续片段 Sentence    └── 句内录音单位 Unit
```

其中：

-   `Asset` 对应原始音频或视频；
-   `Sentence` 由静音检测和人工调整产生；
-   `Unit` 由语言包、ASR/对齐或人工标注产生；
-   五段式的前两项对应 `Sentence + Unit`；
-   后三项只是语言包生成的可读上下文。

我认为下一步应讨论的是：**一个句内 Unit 应该只对应一个主要时间范围，还是允许同一个 Unit 包含多个可选切片范围**。后者能支持同一个字内部同时保存 CV、VC、稳定元音段等多个声学区域，但数据结构会稍复杂。

  

#### 你说：

每个字都有独立的编号，但其他仅支持一个字的格式，参考原版人力V助手的质量排序与jrkhh的手动排序。

  

#### ChatGPT 说：

思考了 55s

明白了。这里应该采用\*\*“一字一条目、多条目同音分组排序”\*\*，而不是“一个字下面再挂多个切片”。

## 数据粒度正式确定

```
原素材 Asset└── 连续片段 Sentence    └── 单字 Unit
```

每个字只有一个永久编号：

```
句编号 + 句内字编号
```

例如原片段：

```
你   好   啊1-1 1-2 1-3
```

对应五段式：

```
1-1-R-ni-hao1-2-ni-hao-a1-3-hao-a-R
```

每个 Unit：

-   对应原素材中的一个字；
-   对应一个主要时间范围；
-   不在内部再拆成多个 CV、VC 或稳定元音切片；
-   CV、CVVC、VCV 只是从这个字生成的兼容原音设定。

## 同一个发音可以有多个独立字条目

例如素材中多次出现“好”：

```
1-2-ni-hao-a5-4-wo-hao-ma8-1-R-hao-jiu
```

它们是三个完全独立的 JRH Unit：

```
1:25:48:1
```

但都属于候选组：

```
hao
```

因此不是：

```
hao├── 切片A├── 切片B└── 切片C
```

而是：

```
独立 Unit 1:2 ─┐独立 Unit 5:4 ─┼─ 都可用于 hao独立 Unit 8:1 ─┘
```

这也更符合原版人力 V 助手按词条收集多个样本的思路。

## 自动质量排序与人工排序合并

旧项目的 `quality_scorer.py` 已经实现了三项自动评分：

-   时长适宜程度；
-   RMS 音量稳定性；
-   F0 音高稳定性。

默认权重是时长 0.3、RMS 0.3、F0 0.4，并计算加权综合分；而较早的 `bank_sort.py` 则只是把同一词条的样本按时长降序排列。[GitHub+1](https://github.com/TNOTawa/JinrikiHelper/blob/main/src/quality_scorer.py)

新项目目前的人工排序逻辑，则是把同一个基础别名的条目组成一组，允许用户调整组内顺序。调整后，第一项使用无编号别名，后续自动变成 `base1`、`base2` 等；底层也已经有拖拽列表和 `reorder_group_entry()`。[GitHub+1](https://github.com/TNOTawa/utau-jrkhh/blob/main/src/draggable_list.py)

新 JRH 可以直接继承这两个思路：

```
首次生成候选组↓按自动质量评分排序↓用户试听↓手动拖拽修正↓保存人工顺序
```

核心原则是：

> **自动评分负责给出初始建议，人工排序是最终决定。**

## 推荐 JSON

单字自身保存客观信息和自动评分：

JSON

```
{  "sentence_id": 5,  "unit_id": 4,  "language": "zh-CN",  "unit_system": "pinyin-toneless",  "label": "hao",  "display": {    "previous": "wo",    "current": "hao",    "next": "ma"  },  "source": {    "asset_id": "video-001",    "start_sample": 582400,    "end_sample": 601200  },  "quality": {    "combined": 0.87,    "metrics": {      "duration": 0.92,      "rms_stability": 0.81,      "f0_stability": 0.88    },    "scorer": "jrh-basic-quality",    "scorer_version": "1.0"  }}
```

分组顺序单独保存：

JSON

```
{  "candidate_orders": {    "zh-CN:pinyin-toneless:hao": {      "mode": "manual",      "unit_ids": [        "5:4",        "1:2",        "8:1"      ]    }  }}
```

这里不必保存 `auto_order`，因为它可以随时根据 `quality.combined` 重新计算。

当用户尚未手动排序时：

JSON

```
{  "mode": "automatic"}
```

一旦用户拖拽过：

JSON

```
{  "mode": "manual",  "unit_ids": ["5:4", "1:2", "8:1"]}
```

还可以提供“恢复自动排序”按钮。

## 导出传统格式时，排序直接决定编号

假设 `hao` 组人工顺序为：

```
5:41:28:1
```

### JRH 五段式

因为五段式本身唯一，不需要附加排序编号：

```
5-4-wo-hao-ma1-2-ni-hao-a8-1-R-hao-jiu
```

### CV 导出

```
hao   → 5:4hao1  → 1:2hao2  → 8:1
```

### CVVC 或 VCV 导出

也采取相同原则：

```
i hao    → 当前组第一候选i hao1   → 当前组第二候选i hao2   → 当前组第三候选
```

因此人工排序不仅影响编辑器显示，也决定：

-   哪个样本获得无数字的主别名；
-   同音素材的数字后缀顺序；
-   普通音素器默认先使用哪个；
-   DAW 搜索结果的排列顺序。

这正好延续当前 JRKHH 的操作习惯。

## 自定义音素器不能简单使用组内第一项

JRH 自定义音素器拥有上下文信息，所以它的逻辑应该比传统 CV 音素器更智能。

例如目标是：

```
ni → hao → a
```

候选有：

```
1-2-ni-hao-a    人工排序第 35-4-wo-hao-ma   人工排序第 18-1-R-hao-jiu   人工排序第 2
```

虽然 `5:4` 的人工顺序最高，但 `1:2` 完整匹配前后上下文，因此应优先选择：

```
1-2-ni-hao-a
```

所以排序层次建议是：

```
第一层：上下文及连续性匹配等级第二层：人工候选顺序第三层：自动质量分第四层：永久编号，保证结果稳定
```

更具体地说：

1.  能直接使用原句连续字串；
2.  前后上下文都匹配；
3.  匹配一侧上下文；
4.  仅当前无声调拼音匹配；
5.  同辅音或同元音的降级候选；
6.  其他语言包定义的替代关系。

在同一匹配等级里，优先人工排序；未人工排序时再按质量分。

这样同时满足：

-   自定义音素器优先流畅连续；
-   用户仍然拥有最终的音色和质量偏好控制权；
-   自动评分不会强行覆盖人的听感判断。

## CV、CVVC、VCV 的参数处理

一个 Unit 只有一个字和一段来源音频，但可以有不同目标格式的原音设定参数：

JSON

```
{  "sentence_id": 5,  "unit_id": 4,  "source": {    "start_sample": 582400,    "end_sample": 601200  },  "target_settings": {    "jrh": {      "offset": 0,      "consonant": 105,      "cutoff": -30,      "preutterance": 82,      "overlap": 28    },    "cv": {      "offset": 18,      "consonant": 96,      "cutoff": -42,      "preutterance": 75,      "overlap": 20    },    "cvvc": {      "offset": 12,      "consonant": 102,      "cutoff": -35,      "preutterance": 80,      "overlap": 25    }  }}
```

这不是一个字含有多个“素材切片”，而是：

> **同一个字、同一段素材，拥有多个目标格式的参数视图。**

初期甚至可以只保存一套默认参数，导出时自动转换；只有用户专门调整某种格式后，才生成对应的覆盖参数。

## 最终模型

```
Unit├── 永久编号：5:4├── 当前录音单位：hao├── 可读上下文：wo-hao-ma├── 唯一来源时间范围├── 自动质量评分└── 各导出格式参数覆盖Candidate Group：hao├── 5:4  ← 人工第1├── 1:2  ← 人工第2└── 8:1  ← 人工第3
```

所以之前“一个 Unit 是否保存多个可选切片”的问题可以直接否定：

> **不保存。每个字就是一个独立 Unit；多个可选素材表现为多个独立 Unit 进入同一候选组，再通过自动质量排序和人工排序确定优先级。**

下一步需要定的是：自动质量分是否继续沿用“时长、RMS、F0”三项，还是补充削波、底噪、信噪比、边界完整度等指标。我认为旧三项可以保留，但不能单靠它们判断人力音源质量。

来源

  

#### 你说：

仅留取时长和RMS，机器只是辅助，错误率高，无需倾注过多算力，还是以人工为最终评判标准。

  

#### ChatGPT 说：

思考了 5s

同意。这里不需要做“智能质量判定”，只做**低成本的机械辅助排序**即可。

## 排序原则

最终优先级应当是：

```
人工排序 > 自动初始排序 > 固定编号兜底
```

机器只在用户尚未调整时提供一个初始顺序。一旦用户手动拖动过，该候选组就进入人工模式，后续重新分析也不能自动覆盖。

JSON

```
{  "group": "hao",  "order_mode": "manual",  "units": ["5:4", "1:2", "8:1"]}
```

用户可以主动点击“恢复自动排序”，但程序不能擅自恢复。

## 只保留两个机器指标

### 1\. 有效发声时长

不一定等于整个字的切片长度，最好记录两个值：

JSON

```
{  "duration_ms": 436,  "voiced_duration_ms": 328}
```

不过若为了简单，第一版只保存最终字切片的 `duration_ms` 也足够。

时长主要用于排除：

-   明显过短、可能切残的素材；
-   过长、包含大量空白或相邻字的素材；
-   同音候选中更适合作为默认项的长度。

它不应判断“越长越好”，只需根据语言包设置一个宽松的推荐区间。

例如：

JSON

```
{  "recommended_duration_ms": {    "min": 150,    "max": 900  }}
```

区间内不必细分谁更优秀，避免伪精确。

### 2\. RMS

RMS 更适合直接作为响度参考，而不是复杂的“稳定性评分”。

JSON

```
{  "rms_dbfs": -18.7}
```

用途主要是：

-   找出过轻素材；
-   找出明显过响或可能削波的素材；
-   让同组候选按大致音量接近程度排列；
-   在界面中辅助用户比较。

同样不应把“声音越大”视为质量越高。可以只设宽松推荐范围：

```
正常偏轻偏响
```

而不是输出看似精确的 `87.53 分`。

## 不建议再计算综合质量分

可以不再保存：

JSON

```
"quality_score": 0.8732
```

因为时长和 RMS 很难可靠合成为“质量”。

更合理的是保留原始指标：

JSON

```
{  "analysis": {    "duration_ms": 436,    "rms_dbfs": -18.7  }}
```

自动排序时临时应用规则：

```
1. 明显异常的样本靠后；2. 正常区间内，按语言包设定排序；3. 分不出差异时，按永久编号排序。
```

例如：

```
正常时长、正常 RMS正常时长、偏轻 RMS过短过长
```

但这只是第一次展示的顺序。

## 界面上不要称为“质量评分”

可以显示：

```
时长：436 msRMS：-18.7 dBFS自动建议：正常人工顺序：第 2
```

而不要显示：

```
AI 质量：92 分
```

因为后者会让用户误以为机器真的判断了咬字、音色、连贯性和可用性。

人工试听时真正判断的仍然是：

-   字是否完整；
-   发音是否正确；
-   咬字是否自然；
-   背景是否干净；
-   音色是否合适；
-   与其他候选拼起来是否流畅。

这些都不交给机器打分。

## 自动排序也可以更简单

候选组第一次出现时：

```
明显异常项靠后↓其余按时长排序↓时长接近时按 RMS↓再按句编号和字编号
```

并允许用户选择初始排序策略：

```
按时长按 RMS按原素材顺序按编号
```

“自动建议排序”只是其中一个快捷选项，不应成为强制标准。

## JSON 最终可以很轻

JSON

```
{  "sentence_id": 5,  "unit_id": 4,  "label": "hao",  "analysis": {    "duration_ms": 436,    "rms_dbfs": -18.7,    "analysis_version": 1  }}
```

人工候选顺序则单独保存：

JSON

```
{  "candidate_group": "hao",  "order_mode": "manual",  "ordered_units": [    "5:4",    "1:2",    "8:1"  ]}
```

这符合整个项目的定位：

> **机器负责减少重复劳动，用户负责作出最终判断。**

而且时长与 RMS 都可以非常快地批量计算，不需要 F0 模型、神经网络或 GPU，也不会再增加依赖维护成本。

接下来比较关键的是讨论**人工排序究竟按“当前字”分组，还是按“当前字＋前后文匹配层级”分别排序**。我倾向于人工只维护当前字的总排序，前后文匹配由音素器运行时优先处理，避免用户维护大量细分组。

  

#### 你说：

不同素材特点不同，时长和RMS匹配范围可以根据整体的中位数和平均数、方差等进行计算。人工依然只维护‘当前字‘分组，同现有的jrkhh。

  

#### ChatGPT 说：

思考了 24s

对，这样最合理：**人工始终只维护“当前字”候选组，机器根据整个音源自身的统计分布生成初始建议，不建立前后文子分组。**

## 自适应统计，而非固定阈值

每个普通单字只计算两个原始量：

JSON

```
{  "duration_ms": 436,  "rms_dbfs": -18.7}
```

然后根据当前 JRH 音源已经确认的全部普通单字，计算：

```
时长：均值、中位数、方差、标准差、MAD、分位数RMS：均值、中位数、方差、标准差、MAD、分位数
```

呼吸音、静音、删除项、人工标记为不可用的条目不参与总体统计。

## 建议以中位数为主

均值和方差可以保留，但自动排序最好主要参考：

```
中位数 + MAD
```

因为人力素材中很容易存在：

-   切得特别长的整字；
-   被切残的极短字；
-   音量异常大的素材；
-   几乎无声的误标注。

这些异常值会明显拉偏均值和方差，但对中位数影响较小。

可以采用稳健偏差：

```
robust_z = 0.6745 × (当前值 − 中位数) / MAD
```

但这个数值只供程序内部使用，界面不必展示复杂统计，只显示：

```
时长：436 ms　接近音源常态RMS：-18.7 dBFS　略低于音源常态
```

## 不生成虚假的“质量总分”

程序可以分层，但不要显示“质量 89 分”。

例如：

```
A：时长和 RMS 都处于常态范围B：其中一项略有偏离C：存在明显偏离D：疑似切分异常或接近无声
```

这只是自动排序等级，不表示听感质量。

同一等级内再比较与音源中位数的距离：

```
先比较时长偏差再比较 RMS 偏差最后按句编号和字编号稳定排序
```

也可以保留 JRKHH 类似的快捷排序：

```
自动建议按时长按 RMS按原素材顺序
```

## 不同原素材的音量差异

这里最好采用两级统计：

```
整个音源统计└── 单个原素材 Asset 的局部统计
```

因为从不同视频、录音和歌曲提取的素材，整体增益可能完全不同。一个素材的正常 RMS 是 `-24 dBFS`，另一个可能是 `-14 dBFS`，直接相互比较会误判。

默认逻辑可以是：

1.  某个原素材拥有足够多的有效字时，优先判断它是否符合该素材自身的常态；
2.  原素材样本不足时，退回整个音源的统计；
3.  全局统计主要用于识别特别极端的异常；
4.  不自动归一化或修改音频，只提供排序提示。

时长通常可以更多参考全音源统计；RMS 则更应该参考原素材局部统计。

## 人工排序仍只维护当前字

例如所有 `hao`：

```
1:2  ni-hao-a5:4  wo-hao-ma8:1  R-hao-jiu
```

人工界面仍然只有一个 `hao` 分组：

```
hao├── 5:4　人工第 1├── 1:2　人工第 2└── 8:1　人工第 3
```

不再建立：

```
ni-hao-a 分组wo-hao-ma 分组R-hao-jiu 分组
```

音素器运行时自己判断上下文和连续性。候选匹配等级相同时，才参考用户维护的 `hao` 总排序：

```
原素材连续性＞前后文匹配程度＞当前字人工排序＞时长/RMS 自动建议＞固定编号
```

这样人工维护量与现有 JRKHH 基本一致，但自定义音素器能够利用 JRH 额外保存的上下文信息。

## 推荐保存方式

单字只保存原始分析结果：

JSON

```
{  "sentence_id": 5,  "unit_id": 4,  "label": "hao",  "analysis": {    "duration_ms": 436,    "rms_dbfs": -18.7  }}
```

统计结果作为可重新生成的缓存：

JSON

```
{  "analysis_summary": {    "revision": 12,    "global": {      "duration_ms": {        "count": 1834,        "mean": 412.6,        "median": 386.0,        "variance": 12842.1,        "mad": 74.0      },      "rms_dbfs": {        "count": 1834,        "mean": -19.4,        "median": -18.9,        "variance": 18.2,        "mad": 2.7      }    }  }}
```

用户修改切分边界、删除条目或新增素材后，只需让缓存失效并重新统计。计算量是线性的，不需要模型，也几乎不会造成等待。

这样机器的角色很明确：

> **根据这个音源自己的分布，帮助用户先把明显异常项靠后；真正哪个素材最好，完全由人工试听和拖拽排序决定。**

下一步可以继续确定的是原音设定层：JRH 是否只保存一套人工原音参数，再由 CV、CVVC、VCV 编译器换算；还是允许用户分别人工调整各格式的覆盖参数。

  

#### 你说：

只存一套然后换算。其他的传统方案终究只是主动适配，核心维护的还是我们的自定义音素器。

  

#### ChatGPT 说：

思考了 20s

这就能把维护面压得很小：

> **每个字只维护一套 JRH 原生原音设定；五段式音素器直接使用它，CV、CVVC、VCV 只由编译器换算，不允许逐格式人工维护。**

## 建议固定为三层

```
JRH 母版├── 单字永久编号├── 原素材时间轴├── 唯一一套原音设定├── 当前字人工排序└── 上下文与语言单位主要运行目标└── JRH 五段式 + 自定义音素器传统兼容目标├── CV├── CVVC└── VCV
```

传统格式生成完即是编译产物，可以随时删除重建，不回写 JRH 母版。

## 唯一原音设定最好用绝对采样点保存

界面仍然可以显示 UTAU 熟悉的五参数，但 JSON 底层建议记录原素材上的位置：

JSON

```
{  "sentence_id": 1,  "unit_id": 2,  "timing": {    "start_sample": 125400,    "overlap_sample": 126100,    "preutterance_sample": 128300,    "consonant_end_sample": 133500,    "end_sample": 153200  }}
```

这样：

-   导出独立 WAV 时可以换算成 `offset/cutoff`；
-   直接引用原长音频时也不会丢失位置；
-   音视频回轨使用同一套坐标；
-   未来其他软件不需要先理解 `oto.ini` 的相对参数；
-   更换导出采样率时也可以重新换算。

用户编辑时仍可使用熟悉的：

```
左空白 / 固定范围 / 右空白 / 先行发声 / 重叠
```

只是保存时转成统一时间轴坐标。

## 传统格式的差异放进“转换规则”

不要放进每个字的数据里，而是放在导出器配置中：

JSON

```
{  "export_profile": "utau-cvvc",  "rules": {    "vc_preutterance_ratio": 0.5,    "minimum_vc_duration_ms": 60,    "use_previous_unit_context": true  }}
```

也就是：

```
单字数据不变+编译目标规则=目标 oto.ini
```

不同 CVVC 习惯、VCV 命名方式或语言社区规范，可以做成多个导出预设，但不增加母版维护量。

## 自定义音素器不受传统排序方式限制

核心流程仍然是：

```
目标歌词单位↓搜索当前字候选↓优先原素材连续片段↓比较前后文匹配↓参考当前字人工排序↓选择永久编号↓返回五段式别名
```

例如最终选中 `1:2`，音素器直接返回：

```
1-2-ni-hao-a
```

它不需要关心这个字在 CV 导出里叫 `hao`，在 VCV 中叫 `i hao`，还是在 CVVC 中额外生成了 `ao a`。

## 传统格式只保证“尽可能适配”

这一点最好在规范里明确：

-   JRH 五段式是完整原生目标；
-   CV、CVVC、VCV 是兼容编译；
-   某些格式可能丢失连续上下文和永久编号信息；
-   自动换算不保证等同于专门人工制作的传统音源；
-   导出器可以报告无法合理生成的条目，但不要求用户逐项修正。

例如 CVVC 缺少足够的过渡区时，可以：

```
生成近似条目标记为降级转换或跳过该条目
```

而不是把这些特殊情况重新塞回 JRH 核心。

## UI 也应该围绕这一原则设计

用户只看到一套可编辑原音设定：

```
原生 JRH 原音设定
```

CV、CVVC、VCV 页面只提供：

```
导出预览命名规则转换参数冲突检查重新生成
```

不能让用户在兼容页面逐字调整，否则项目很快又会变成同时维护四套音源。

至此，JRH 的核心定位可以概括为：

> **以原素材连续片段和单字永久坐标为基础，以人工原音设定与人工候选排序为权威，以五段式音素器为主要使用方式，以传统 UTAU 格式为自动生成的兼容层。**

下一步最需要确定的是：一套原音设定在界面上继续完全沿用 UTAU 五线参数，还是采用更直观的“字起点、先行发声点、辅音结束点、字终点”等时间标记，再实时换算成 UTAU 参数。

  

#### 你说：

我们模仿VCV的格式，即前一个音的元音作为先行，衔接到这个音。应该可以实现吧？

  

#### ChatGPT 说：

思考了 48s

可以，而且这应该成为 **JRH 原生原音设定的核心方式**：

> 每个字仍然拥有独立编号，但它对应的原音窗口从“前一字的元音尾部”开始，经过字间过渡，衔接到当前字。

例如原素材：

```
你       好       啊ni      hao      a
```

`好（1-2）` 的原音范围不是只截取 `hao`，而是：

```
前一个 i 的稳定尾部 → i-h 过渡 → h → ao 稳定部分
```

这就是类似 VCV 的：

```
i hao
```

## 五段式与实际先行音可以分开

五段式仍然可以保持人类易读：

```
1-2-ni-hao-a
```

JSON 另外保存真正用于衔接的前置元音：

JSON

```
{  "sentence_id": 1,  "unit_id": 2,  "display": {    "previous": "ni",    "current": "hao",    "next": "a"  },  "transition": {    "leading_vowel": "i"  }}
```

或者编译时显示成：

```
1-2-i-hao-a
```

两种都可以，因为音素器真正认的是：

```
1-2
```

后三项只是可读说明，不参与身份判断。

## 一字一个窗口，但相邻窗口允许重叠

同一句的原音设定大致会是：

```
1-1：静音 → ni1-2：i 尾部 → hao1-3：ao 尾部 → a
```

时间轴上：

```
[------ 1-1 ------]             [------ 1-2 ------]                           [------ 1-3 ------]
```

相邻 Unit 的范围重叠是正常且必要的，不能把原素材机械切成互不重叠的小块。

最适合的导出方式可能是：

```
sentence_1.wav
```

然后在同一个 WAV 上建立多条 `oto.ini`：

```
sentence_1.wav=1-1-R-ni-hao,...sentence_1.wav=1-2-ni-hao-a,...sentence_1.wav=1-3-hao-a-R,...
```

这样不需要复制大量重复音频，也能保留原句的连续关系。

## 唯一一套原音参数

每个字维护一套 VCV 风格的时间标记：

JSON

```
{  "window_start": 125400,  "overlap": 126100,  "preutterance": 128300,  "fixed_end": 133500,  "window_end": 153200}
```

含义可以定义为：

```
window_start：前一元音稳定尾部的取样起点overlap：开始与前一音交叠的位置preutterance：当前字辅音或当前发音的起始点fixed_end：辅音及过渡结束、进入当前稳定元音的位置window_end：当前字可延伸部分的结束位置
```

对于 `i hao`：

```
window_start↓iiiiii | h | aooooooo       ↑   ↑preutterance           fixed_end
```

界面仍然可以显示为传统 UTAU 的五线原音设定，只是 JRH 底层保存绝对时间点。

## 自定义音素器的选取逻辑

目标歌词是：

```
ni → hao → a
```

选择 `hao` 时：

1.  优先寻找原素材中连续的 `ni-hao-a`；
2.  其次寻找前置元音为 `i` 的 `i hao`；
3.  再寻找同元音类别的过渡；
4.  最后才选择普通 `hao` 候选；
5.  同等级候选参考用户对 `hao` 分组的人工排序。

最终选中：

```
1:2
```

然后返回：

```
1-2-ni-hao-a
```

OpenUtau 的 Phonemizer 接口能够获取前后相邻音符，同时输出的 phoneme 字符串需要匹配音源中的 oto alias，因此这种“根据上下文选中永久坐标，再返回五段式别名”的实现路径是可行的。[GitHub](https://github.com/stakira/OpenUtau/blob/master/OpenUtau.Core/Api/Phonemizer.cs?utm_source=chatgpt.com)

## 句首与特殊情况

句首没有前一元音时：

```
R → ni
```

或者底层记录：

JSON

```
"leading_type": "silence"
```

呼吸后：

```
BR → ni
```

辅音结尾、英语辅音簇、鼻音或特殊语言，则由语言包定义“先行单位”。核心层不必强制它永远是普通元音：

```
普通话：前一音节韵母/主要元音日语：前一音拍元音英语：前一音节 nucleus，必要时保留尾辅音自定义语言：语言包自行定义
```

因此，可以将原生模式正式理解为：

> **以当前字为编号主体，以前一字的可延续部分作为先行区，采用 VCV 式重叠衔接。**

这比纯 CV 更适合自然语音素材，也正好能利用 JRH 保存的原句连续时间轴。

来源

  

#### 你说：

当然，不可避免的会出现一些组合音源并没有记录的情况。我记得音素器是可以做到自动拆字拼字的，在我们这套方案上可以实现吗？

  

#### ChatGPT 说：

思考了 1m 22s

**可以实现，而且 OpenUtau 现有的中文 CVVC 音素器已经采用了类似逻辑。**

OpenUtau 的音素器不是只能“一字返回一个原音”，而是可以给一个音符返回多个带位置的 `Phoneme`；每个音素都可以安排在音符起点前后。其内置中文 CVVC 音素器会先寻找完整的“前元音＋当前音节”，找不到时便返回一个位于音符前方的 VC 和一个位于音符起点的 CV。[GitHub+2GitHub+2](https://raw.githubusercontent.com/stakira/OpenUtau/master/OpenUtau.Core/Api/Phonemizer.cs)

## 在 JRH 中的实现方式

每个字仍然只维护一套 VCV 式原音设定，但编译时自动生成三个运行视图：

```
完整视图 FULL前置过渡 TRANSITION当前字主体 BODY
```

例如：

```
1-2-ni-hao-a
```

编译时产生：

```
1-2-ni-hao-a        完整的 i → hao1-2-ni-hao-a_T      前元音 → h 的过渡部分1-2-ni-hao-a_B      hao 的当前字主体
```

`_T`、`_B` 都只是隐藏或派生别名，用户不需要维护，也不属于新的原音设定。

## 完整组合存在时

目标歌词：

```
ni → hao → a
```

而音源中正好存在：

```
1-2-ni-hao-a
```

音素器直接返回一个原音：

```
1-2-ni-hao-a
```

这是最高优先级。

## 完整组合不存在时

假设目标是：

```
wo → hao
```

前一个字的尾元音是 `o`，但音源中没有任何完整的：

```
o hao
```

不过音源可能有：

```
12-6-bo-he-ni
```

其中包含可用的：

```
o → h
```

同时另一个位置有：

```
1-2-ni-hao-a
```

其中包含可用的：

```
hao 主体
```

音素器便可以返回：

```
12-6-bo-he-ni_T     放在当前音符起点之前1-2-ni-hao-a_B      放在当前音符起点
```

逻辑近似：

C#

```
return new Result {    phonemes = new[] {        new Phoneme {            phoneme = transitionAlias,            position = -transitionLength,        },        new Phoneme {            phoneme = bodyAlias,            position = 0,        },    },};
```

OpenUtau 自带 CVVC 音素器同样会把 VC 放在负位置，把 CV 放在音符起点。[GitHub+1](https://raw.githubusercontent.com/stakira/OpenUtau/master/OpenUtau.Plugin.Builtin/ChineseCVVCPhonemizer.cs)

## 需要编译器预先生成派生别名

这里有一个重要边界：

> 音素器可以决定使用哪些片段、以什么顺序播放，但它返回的每个名称都必须对应已有的 oto alias。

OpenUtau 的 API 明确要求 `Phoneme.phoneme` 匹配音源中的 oto alias；音素器也不应在运行期间修改歌手数据。[GitHub](https://raw.githubusercontent.com/stakira/OpenUtau/master/OpenUtau.Core/Api/Phonemizer.cs)

所以不能等唱到某个字时，再由音素器临时切 WAV、修改 `oto.ini`。正确结构是：

```
JRH 母版↓编译阶段├── 生成完整五段式别名├── 生成过渡片段别名├── 生成当前字主体别名└── 生成结束音别名↓OpenUtau 音素器只负责搜索和组合
```

这些派生条目都能从同一套原生时间标记换算出来，不增加用户维护量。

## 一套原音标记如何拆出两部分

例如一个 VCV 式窗口：

```
iiiiii | h | aooooooo
```

对应时间点：

```
窗口起点重叠点当前发音起点当前元音稳定点窗口终点
```

可以派生为：

```
FULL：前一元音尾部 → 当前字结尾TRANSITION：前一元音尾部 → 当前辅音及过渡结束BODY：当前辅音开始 → 当前字结尾
```

这三者都引用原来的连续片段 WAV，只是 `oto.ini` 参数不同，不必生成三份音频。

## 推荐的回退顺序

音素器可以按照以下层级查找：

1.  **原素材中连续的相邻字**；
2.  **前元音＋当前字完全匹配的完整窗口**；
3.  **前元音＋当前辅音的过渡片段，再拼当前字主体**；
4.  **相近过渡片段，再拼当前字主体**；
5.  **只使用当前字主体，退化为 CV**；
6.  找不到当前字时报告缺音。

同一层级存在多个候选时，再依次参考：

```
原素材连续性→ 当前字人工排序→ 时长和 RMS 辅助顺序→ 永久编号
```

人工仍然只维护 `hao` 分组，不需要维护 `o-h`、`i-h` 等过渡组。过渡片段由语言包根据“前元音＋当前声母”自动建立索引。

## 需要区分两种“没有记录”

### 没有记录特定上下文组合

例如有 `hao`，但没有 `o hao`。

这种情况非常适合自动拆分：

```
o-h 过渡 + hao 主体
```

应该作为 JRH 音素器的标准能力。

### 连当前字本身都没有

例如整个音源没有任何 `hao`，只有 `ha`、`ao`、`hong`。

理论上还能继续拆成：

```
h + ao
```

但这已经属于更深层的音素拼接：

-   拼接痕迹明显；
-   不同声母和韵母的协同发音可能不兼容；
-   汉语、日语和英语规则差异较大；
-   搜索路径会迅速复杂化。

因此第一版建议到“过渡＋完整当前字主体”为止：

```
VC + CV
```

不要默认进行：

```
C + V
```

更深层拆音可以以后作为语言包的实验性降级策略。

## 最适合 JRH 的最终模式

```
优先：完整 VCV 式五段原音   ↓ 缺失回退：过渡片段 + 当前字主体   ↓ 缺失兜底：当前字主体   ↓ 缺失报告缺字
```

因此答案是：\*\*不仅能实现，而且与 OpenUtau 的音素器机制天然匹配。\*\*JRH 的优势在于，过渡片段和主体片段不需要用户额外标注第二套数据，而是由编译器从唯一一套原生时间标记中自动派生。

来源

  

#### 你说：

从这个角度看，我们这套方案在进入openutau编辑器后的各种极端条件（缺字漏字等）下都有什么优势，以及对比传统人力的拼字CV音源有什么提升。

  

#### ChatGPT 说：

思考了 37s

从这个角度看，你们这套方案最大的价值可以概括成一句话：

> **它把 VCV 的自然衔接上限、CV 的基础覆盖能力、以及整句素材的连续性优势放进了同一个音素器里。**

理想情况下使用完整连续原音；组合缺失时自动拆分；再差则退化成普通 CV。传统人力拼字 CV 基本从一开始就在最后一级。

## 一、在 OpenUtau 中的分级回退

建议固定为：

```
1. 原素材连续字串2. 完整的“前元音＋当前字”3. 过渡片段＋当前字主体4. 仅当前字主体，退化为 CV5. 可选的深层音素拼接6. 明确报告缺音
```

例如要唱：

```
ni → hao → a
```

### 最佳情况：原句中存在

音源原素材正好有：

```
ni hao a
```

音素器连续选取：

```
1-11-21-3
```

三个字来自同一句、时间连续，衔接最自然。

### 没有完整原句，但有对应 VCV

其他素材里存在：

```
i hao
```

就直接使用完整 VCV 式窗口。

### 没有 `i hao`

程序自动寻找：

```
i-h 过渡 + hao 主体
```

两个原音安排在同一个音符内。

### 连 `i-h` 也没有

退化为：

```
hao 主体
```

相当于传统 CV。

因此，在当前字本身存在的前提下：

> **JRH 最差可以退回 CV，但最好可以达到连续原句或 VCV。**

## 二、各种极端情况的表现

| OpenUtau 中的情况 | JRH 方案 | 相比传统 CV |
| --- | --- | --- |
| 完整上下文存在 | 优先使用同句连续原音 | 保留真实协同发音 |
| 前后字组合不存在 | 换用相同前元音的完整窗口 | 不要求录全所有词组 |
| 前元音＋当前字也不存在 | 过渡和当前字主体拆开拼接 | 比直接 CV 更平滑 |
| 只有当前字存在 | 退化为 BODY/CV | 覆盖能力不低于 CV |
| 同一个字有很多素材 | 上下文优先，再参考人工排序 | 不再固定使用一个默认样本 |
| 连续快速音符 | 优先选择原素材中的连续路径 | 减少每个字随机跳素材 |
| 长音  | 拉伸当前字稳定元音主体 | 不必把前置过渡一起拉长 |
| 极短音 | 可缩短主体，但尽量保留辅音过渡 | 辅音时序比纯 CV 更可控 |
| 句首  | 使用静音起始型原音 | 等同于 `- CV`，但仍有统一规则 |
| 呼吸后起音 | 可使用呼吸或静音作为前置状态 | 不必强行按普通元音连接 |
| 用户改了前一个歌词 | 音素器重新计算前元音及候选 | 不用手动替换大量别名 |
| ASR 标错字 | 人工修改母版即可重新编译 | 不依赖一次性自动结果 |
| 某个真实汉字没录过 | 只要对应发音单位存在即可使用 | 不依赖字形，只依赖读音 |
| 当前发音单位完全不存在 | 报告缺音，或启用实验性深层拼接 | 这点无法凭空解决 |

## 三、对传统人力 CV 最直接的提升

### 1\. 从“只保存字”提升为“保存真实过渡”

传统 CV 的 `hao` 通常主要保存：

```
h → ao
```

前一个字的元音与 `h` 之间的连接由重叠、交叉淡化和重采样器处理。

JRH 的完整原音则保存：

```
前一元音尾部 → h → ao
```

例如：

```
i → h → ao
```

这段过渡是真实说话或演唱中产生的，而不是编辑器临时拼出来的。

因此更容易改善：

-   元音到辅音的断裂；
-   起音过硬；
-   字与字之间明显“一粒一粒”；
-   辅音像突然贴上去；
-   连读时音色突变。

### 2\. 不要求事先录齐完整 VCV 表

传统 VCV 音源的最大问题之一，是组合数量大。

JRH 不要求：

```
a haoi haou haoe haoo hao
```

全部存在。

有完整组合就使用；缺失时拆成：

```
V-C 过渡 + 当前字主体
```

所以它不是传统意义上的“必须录齐 VCV”，而是：

> **稀疏 VCV 素材库＋动态 CVVC 式回退。**

### 3\. 同一个字不再只有一个固定样本

传统 CV 导出后可能是：

```
haohao1hao2
```

普通音素器通常默认使用 `hao`，其他候选需要用户手工改别名。

JRH 音素器可以在多个 `hao` 之间动态选择：

```
上下文匹配→ 原素材连续性→ 人工排序→ 时长和 RMS 辅助
```

因此同一个字在不同歌词环境中，可以使用不同来源。

### 4\. 编辑歌词后可以重新选择，而非推倒重拼

例如原来是：

```
ni → hao
```

后来用户改成：

```
wo → hao
```

传统人力 CV 基本还是使用相同的 `hao`。

JRH 会重新判断：

```
前一个尾元音从 i 变成 o
```

然后重新搜索：

```
o hao
```

或者：

```
o-h + hao
```

这让音源对歌词修改具有适应能力。

### 5\. 能进行整段路径选择

传统音素器通常逐字处理：

```
当前字最好的是谁？
```

JRH 可以考虑整段：

```
这一串字怎样选，整体跨素材次数最少？
```

例如两个方案：

```
方案 A：字1最好样本 + 字2最好样本 + 字3最好样本来自三个不同视频方案 B：三个样本单独评分略低但全部来自同一句连续素材
```

实际听感很可能方案 B 更自然。

所以最终不应该只是逐字贪心，而应允许全句或局部动态规划。

## 四、缺字、漏字时的具体优势

这里要区分三种“缺”。

### 1\. 缺少真实字形，但不缺发音

例如音源素材中没说过“豪”，但说过“好”，两者在无声调拼音表中都映射到：

```
hao
```

音素器仍然可以直接使用。

传统基于录音表的 CV 也能做到，但 JRH 还会根据前后音选择更合适的 `hao`。

### 2\. 有当前音节，但缺少指定过渡

例如有 `hao`，但没有 `o hao`。

JRH 可以：

```
o-h + hao
```

传统 CV 只能：

```
hao
```

这是最实用的提升之一。

### 3\. 当前音节本身完全不存在

例如整个音源没有任何 `hao`。

此时理论上可以尝试：

```
h + ao
```

但质量风险明显高于前两级，因为辅音和韵母之间存在真实协同发音。

因此更稳妥的默认策略是：

```
尝试语言包允许的替代读音↓仍无结果则明确标红缺音
```

深层 `C＋V` 拼接可以做成实验开关，而不作为默认承诺。

## 五、对长短音符的优势

### 长音

完整 VCV 窗口可以拆成：

```
先行过渡当前字辅音稳定元音主体
```

需要延长时，主要延长稳定元音，不拉伸前面的真实过渡。

传统 CV 也能依赖固定区实现，但 JRH 对各区段的语义更清楚，编译器和音素器更容易配合。

### 短音

音符很短时可以优先保留：

```
前置过渡＋辅音
```

并缩短稳定元音部分。

但如果音符短到连辅音都放不下，这不是格式可以完全解决的，仍需要 OpenUtau 的辅音速度、先行发声和音符长度共同处理。

## 六、可重复性与人工控制

五段式前两项是永久坐标：

```
12-6
```

因此即使后三项改变：

```
12-6-bo-he-ni12-6-o-he-ni
```

音源内部仍然知道是同一个字。

这带来几个传统 CV 不容易做到的优势：

-   同一工程重新编译后仍能定位原素材；
-   可以解释为什么选中了这个样本；
-   可以锁定某个具体来源；
-   用户人工排序不会因别名重编号而失效；
-   修改拼音显示规则不影响真实身份；
-   可以从 OpenUtau 原音追溯回原视频时间轴。

## 七、维护成本上的提升

传统人力 CV、CVVC、VCV 往往意味着维护多套：

```
别名oto 参数重复样本编号缺音表特殊命名规则
```

JRH 只维护：

```
一套单字标注一套 VCV 式原音设定一套当前字人工排序
```

其他全部编译：

```
JRH 原生别名BODYTRANSITIONCVCVVCVCV
```

所以核心复杂度放在一次性的编译器和音素器里，而不是让每个音源制作者重复承担。

## 八、最重要的实际结论

与传统人力拼字 CV 相比，JRH 的提升不是简单的“音质一定更好”，而是：

### 音质上限更高

存在连续上下文时，可以使用真实 VCV 或原句连接。

### 覆盖下限不降低

上下文缺失时，仍能退回当前字主体，也就是 CV。

### 缺组合时更有弹性

可以用过渡＋主体组合，而不是直接放弃上下文。

### 人工工作量没有按组合数膨胀

用户仍然只标一个字、维护当前字分组。

### 编辑器内更智能

歌词变化后，音素器可以重新计算，而不是要求用户重新拼字。

### 结果更可追溯

每个实际播放原音都能回到唯一的句编号、字编号和原素材时间轴。

它的准确定位可以写成：

> **一种以连续自然语音为素材、以单字为人工维护单位、以 VCV 为最佳使用方式、以动态拆分保证覆盖、以 CV 作为最终兜底的人力音源体系。**

但也要保留三个现实限制：当前发音完全不存在时不能无损创造；跨素材音色差异仍可能听得出来；唯一一套原音设定自动派生出的过渡和主体不一定每次都完美，仍需要好的人工边界标注。

  

#### 你说：

我们还需要讨论什么细节吗

  

#### ChatGPT 说：

思考了 9s

还需要，但已经从“方向讨论”进入“规格落地”阶段了。真正需要继续定死的主要有以下几项。

## 开工前必须确定

### 1\. 唯一一套原音设定到底是哪五个点

这是目前最关键的细节。需要明确每条单字原音的时间标记含义，例如：

```
窗口起点重叠点当前字起音点当前字稳定段起点窗口终点
```

尤其要确定：

-   前一元音保留多少；
-   `preutterance` 对应哪个声学位置；
-   无辅音纯元音如何标；
-   塞音、擦音、鼻音、英语辅音簇如何处理；
-   句首没有前元音时怎么标。

只要这一层定义不清，后面的完整 VCV、过渡片段、BODY、CVVC 导出都会不稳定。

### 2\. 自动拆分出的派生条目

需要规定每个单字自动编译哪些运行时别名：

```
FULL：前一音尾部＋当前字TRANSITION：前一音尾部＋当前字起音过渡BODY：当前字主体ENDING：当前字尾部，可选
```

还需要决定这些别名：

-   是否写进同一个 `oto.ini`；
-   是否对用户隐藏；
-   命名规则是什么；
-   是否都直接引用原句 WAV；
-   哪些语言需要 `ENDING`。

### 3\. 音素器的精确回退顺序

目前大方向已经有了，但还应写成不可歧义的规则：

```
原句连续→ 完整前置单位＋当前字→ 过渡＋当前字主体→ 当前字主体→ 语言包允许的替代→ 缺音
```

还要确定：

-   连续性与完整上下文谁优先；
-   人工排序在第几层生效；
-   是否按整句动态规划，还是先做局部贪心；
-   用户手动锁定某个候选后，音素器是否停止自动替换；
-   同样输入是否必须永远选出相同结果。

我建议第一版先做**确定性的局部选择＋连续片段奖励**，以后再升级整句动态规划，避免第一版过重。

### 4\. 人工排序除了拖拽还需要什么

人工只维护“当前字”分组已经确定，但建议再增加两个简单状态：

```
置顶禁用
```

例如某个 `hao` 虽然没有删除，但存在爆音或读错，可以标记为：

JSON

```
{  "enabled": false}
```

否则它仍可能被上下文匹配选中。

### 5\. 编号确认后的编辑规则

还需要定清楚：

-   已确认的句被拆分时如何编号；
-   已确认的两个句合并后保留哪个编号；
-   中间插入新字时使用新编号还是允许空号；
-   删除编号是否永久保留；
-   未发布工程是否允许重新整理编号。

我建议：

> 制作阶段可整理；执行“冻结编号”或首次导出发布后，只增不改、不复用。

## 格式规范需要确定

### 6\. JRH 是目录还是单文件

建议同时支持：

```
开发状态：文件夹形式发布状态：ZIP 容器形式的 .jrh
```

还要规定：

-   JSON 还是 JSONL；
-   原素材是否必须包含在包内；
-   是否允许相对路径引用外部视频；
-   如何记录文件哈希；
-   音频重新采样后如何保持原视频时间轴映射。

### 7\. 时间统一使用什么单位

建议底层不要只使用毫秒，而是保存：

```
原始媒体时间戳工作音频采样点工作音频采样率
```

否则视频是 48 kHz、导出 WAV 是 44.1 kHz 时容易累计误差。

### 8\. 语言包接口

语言包至少需要提供：

```
文本 → 录音单位当前单位 → 主元音/韵母当前单位 → 起始辅音前后文 → 可读五段式单位替代关系传统格式别名生成规则
```

例如普通话语言包定义无声调拼音，日语定义罗马音，英语定义 ARPA 或 X-SAMPA。

核心 JRH 不判断 `hao`、`ko`、`HH` 的语言学意义。

## 可以稍后讨论

### 9\. 自动模型扩展协议

包括：

-   ASR Provider；
-   G2P Provider；
-   SOFA Aligner Provider；
-   VAD Provider。

这可以等核心编辑器和 JRH Schema 稳定后再接，否则容易重走旧项目路线。

### 10\. 旧项目迁移

最好最终支持：

```
旧 JinrikiHelper 工程 → JRH现有 oto.ini → JRH现有 JRKHH 人工排序 → JRH 候选顺序
```

但不必作为第一阶段主任务。

### 11\. 传统格式转换的保证级别

建议明确标注：

```
JRH 原生：正式支持CV：稳定兼容CVVC：尽力转换VCV：尽力转换其他格式：实验性
```

不要让兼容层反过来限制 JRH 原生设计。

## 我认为下一轮最应该讨论的内容

就是**唯一一套原音设定的五个时间点及其声学含义**。

这是整个体系最底层的契约。定下来以后，JRH JSON、编辑器波形界面、五段式 `oto.ini`、自动拆字、CV/CVVC/VCV 换算才能统一。

  

#### 你说：

原音设定参考标准的UTAU社区规范，例如我们这里核心参考VCV和CVVC的标法。 别名可以写进同一个oto，均为引用原句WAV。尾部忽略，不是很需要。同时配一个简单的CV版本进去，便于用户模糊搜索已存在的音。 我会更偏向于使用和UTAU一致的ini，或我更喜欢的yaml，但当前还没正式落地可以不说。 其他的稍后讨论或采纳你上文的默认答案。

  

#### ChatGPT 说：

思考了 8s

好的，目前已经足够形成一版 **JRH v0.1 核心规格**，不用再把尚未落地的配置格式讨论得过细。

## 当前确定的方案

### 1\. 素材层级

```
原素材音频/视频└── 静音检测得到的连续片段（句）    └── 每个字一个永久编号
```

每个字使用：

```
句编号 + 句内字编号
```

作为永久唯一坐标，例如 `12:6`。

### 2\. 原音设定

每个字只维护**一套原音设定**，直接参考 UTAU 社区常见的 VCV、CVVC 标法：

```
offsetconsonantcutoffpreutteranceoverlap
```

其核心思想是：

> 原音窗口保留前一音的元音尾部，并衔接到当前字。

JRH 另外保存它在原素材时间轴上的绝对位置，用于音视频回轨；用户编辑体验仍然使用熟悉的 UTAU 五参数。

### 3\. 原句 WAV

一个连续片段对应一个原句 WAV：

```
sentence_001.wav
```

句中的所有字及其派生别名均引用这一个 WAV，只改变 `oto.ini` 参数：

INI

```
sentence_001.wav=1-1-R-ni-hao,...sentence_001.wav=1-2-ni-hao-a,...sentence_001.wav=1-3-hao-a-R,...
```

不必为每个字、每种兼容形式复制音频。

### 4\. 同一个 `oto.ini` 中的别名

建议同一个 `oto.ini` 同时包含三类条目。

**JRH 原生五段式：**

```
1-2-ni-hao-a
```

这是自定义音素器主要返回的别名。

**自动拆拼派生别名：**

```
1-2-ni-hao-a$T1-2-ni-hao-a$B
```

分别作为过渡片段和当前字主体。它们由编译器生成，用户不需要手动维护。具体后缀以后可改，先保留内部命名空间即可。

**简单 CV 搜索别名：**

```
haohao1hao2
```

它们便于用户在 OpenUtau 中模糊搜索、试听或手工指定已有发音，也方便不使用 JRH 音素器时进行基本调用。

CV 重复别名顺序直接沿用现有 JRKHH：

```
人工排序第一名 → hao人工排序第二名 → hao1人工排序第三名 → hao2
```

### 5\. 不生成尾部原音

当前不单独生成：

```
ENDINGV R尾元音释放
```

句尾需要时直接利用当前字原音的尾部。这样可以减少派生条目和音素器分支，符合人力素材的主要使用场景。

### 6\. 音素器回退

默认采用已经讨论出的逻辑：

```
同一句中真实连续的原音↓前元音与当前字匹配的完整原音↓过渡片段 $T + 当前字主体 $B↓简单 CV 主体↓语言包允许的近似替代↓报告缺音
```

人工只维护“当前字”分组，不维护 `i-h`、`o-h` 等细分过渡组。

候选优先级为：

```
连续性和上下文匹配＞当前字人工排序＞时长、RMS 的统计辅助＞永久编号兜底
```

### 7\. 自动辅助

机器只负责：

-   静音检测和初步分句；
-   ASR、G2P、SOFA 对齐草稿；
-   计算时长和 RMS；
-   根据当前音源自身的均值、中位数、方差等给出初始排序。

最终分句、标注、原音设定和候选排序均以人工结果为准。

### 8\. 语言单位

不同语言自行定义，优先采用对应 UTAU 社区熟悉的录音表符号：

```
普通话：无声调拼音日语：罗马音等录音表单位英语：ARPAbet 或 X-SAMPA其他语言：语言包自定义
```

五段式后三项只用于人类阅读；音素器最终通过前两项唯一定位原音。

### 9\. 传统格式定位

CV、CVVC、VCV 都只是主动兼容层：

```
JRH 母版 → 自动编译传统格式
```

不为它们维护独立原音参数，也不允许兼容层反过来限制 JRH 的设计。真正长期维护的是：

```
JRH JSON/配置+ JRH 五段式 oto+ OpenUtau 自定义音素器
```

### 10\. 配置文件暂不定死

目前只规定需要表达的数据，不急着确定使用：

```
INIYAMLJSON或组合形式
```

我个人也认为 YAML 比大型 JSON 更适合人工查看和少量编辑；但格式生态、解析稳定性和包结构可以等写出第一个原型后再决定。

## 当前已经可以封板的核心定义

> JRH 是以原素材连续片段为基础、以单字永久坐标为身份、以 VCV/CVVC 式原音设定为唯一人工参数、以五段式别名和自定义音素器为核心运行方式，并自动附带拆拼别名与简单 CV 搜索别名的人力音源母版格式。

剩余内容，例如内部派生别名的具体命名、配置采用 INI 还是 YAML、深层 `C+V` 拼接是否启用，都可以在实现过程中按前述默认答案处理，不妨碍开始设计 Core 数据模型和第一版编译器。